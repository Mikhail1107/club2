<header>

    <div class="bs-example">
        <nav class="navbar navbar-expand-md navbar-dark bg-dark">
            <a href="{{ path('home') }}" class="navbar-brand">
                <div class="col-md-3">
                    <img class="logo" src="{{ asset('assets/img/logo.png') }}" alt="" />
                </div>
            </a>
            <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav menu-top">
                    <a href="{{ path('home') }}" class="nav-item nav-link">Accueil</a>
                    <a href="{{ path('contact') }}" class="nav-item nav-link">Contact</a>
                    <a class="nav-link nav-item" href="{{ path('fos_user_security_login') }}">Login/admin</a>
                </div>

            </div>
        </nav>
        <span class="header-title-bottom">
              {# si un utilisateur est connecté en tant qu'admin #}
            {% if is_granted('ROLE_ADMIN') %}
            <div class="bouton">
                <a href="{{ path('fos_user_security_logout') }}">Se déconnecter</a>
            </div>
            {% endif %}
        </span>

    </div>

</header>
