{% extends 'base/base.html.twig' %}

{% block main %}

    <h2 class="join">Contact</h2>

    <div class="container">
        <div class="row">
            <div class="col-md-7">
                <div class="map">
                    <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2832.094404434966!2d-0.5887748496327432!3d44.7788806868279!2m3!1f0!2f0!3f0!
                    3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0xd55274c294efbdb%3A0x5ddaf92b7fe54284!2sLa+Tannerie!5e0!3m2!1sfr!2sfr!4v1566120239726!5m2!1sfr!2sfr"
                            width="100%" height="350" frameborder="0" style="border:0" allowfullscreen></iframe>
                </div>
            </div>
            <div class="col-md-5">
                <p class="m_1">L’association « Yoga Gradignan » est responsable des traitements opérés sur le site accessible à l’adresse https://www.yoga-club.com.
                    Les informations recueillies font l’objet d’un traitement informatique à des fins de suivi de votre demande de contact. Les destinataires
                    des données sont les membres du bureau de l'association, et/ou le cas échéant les salariés ou les bénévoles de l’association en charge de cette mission.
                    Vos données à caractère personnel sont conservées dans nos bases le temps nécessaire au traitement de votre demande.</p>
                <div class="address">
                    <p>Club Yoga, Parc La Tannerie</p>
                    <p>33170, Gradignan,</p>
                    <p>France</p>
                    <p>Téléphone: 06 00 22 66 44</p>
                    <p>Fax: 05 00 00 00 00</p>
                    <p>Email: <span>support[at]yoga.com</span></p>
                </div>
            </div>
        </div>

        {# Balise ouvrante <form> #}

        {{ form_start(messageForm, {'attr': {'class': 'form'}}) }}

        {# balise <label> #}
        {{ form_label(messageForm.lastname, 'Nom', {'label_attr': {'class': 'form-label'}}) }}
        {# balise input #}
        {{ form_widget(messageForm.lastname, {'attr': {'class': 'form-input'}}) }}

        {{ form_label(messageForm.firstname, 'Prénom', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(messageForm.firstname, {'attr': {'class': 'form-input'}}) }}

        {{ form_label(messageForm.email, 'Email', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(messageForm.email, {'attr': {'class': 'form-input form-input'}}) }}

        {{ form_label(messageForm.published_at, 'Date', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(messageForm.published_at, {'attr': {'class': 'form-input'}}) }}

        {{ form_label(messageForm.text, 'Message', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(messageForm.text, {'attr': {'class': 'form-input'}}) }}

        <button class="form-submit" type="submit">Envoyer</button>

        {# Balise fermante </form> #}
        {{ form_end(messageForm) }}

    </div>

{% endblock %}
